# 中版：遊戲零快速移動追蹤調整

基準版本：`37674c0`。`OLD` 保留基準，`MID` 做最小修改，`NEW` 保留最新開發版。此文件只描述 MID 分支；請依各分支的 README 下載與操作。

## 為什麼零、一比較容易消失

| 流程 | 遊戲零 | 遊戲一 | 遊戲二、三 |
| --- | --- | --- | --- |
| 模型模式 | VIDEO | IMAGE | IMAGE |
| 模型之後的處理 | FistIdentityTracker | HandIdentityTracker | 直接使用模型的左右手標籤 |
| 首次建立身分 | 三個穩定影格；雙手模式要兩手可見 | 同左 | 無這道穩定確認 |
| 分類信心門檻 | 0.75 | 0.75 | 無相同的額外 0.75 門檻 |
| 舊版位移限制 | 拳心每次 0.18 | 手腕 0.18、指尖 0.20 | 無相同的跳位排除 |

因此「模型找到了手」不一定等於零、一會接受該手。穩定確認、低信心、跳位、身分衝突都可能讓畫面看起來像漏偵測。二、三沒有相同的過濾，不表示模型永遠不漏手。

尚未取得使用者先前影像辨識專案的實際偵測設定，因此不能確定它與本專案的模型差異。快速移動是否真的造成模型漏偵測，也尚未以真人資料確認。

## MID 只調整遊戲零的兩個條件

1. 拳心允許位移改為考慮距上次接受的時間：`min(0.40, 0.18 + 2 × max(0, dt − 1/30))`。較低偵測幀率時不再以固定 0.18 一律拒收；參數是這次測試初值。
2. 已確認拳頭在 0.35 秒內恢復有效結果、且未發生身分衝突等硬性拒收時，不必再等待三幀。首次／長時間缺失仍需要穩定確認。

左右身分、兩手重疊、座標有效性與分類信心門檻保持；不補假點。共用 HandIdentityTracker 只加入擴充點，遊戲一預設門檻及行為維持原樣。動畫、音效、計分及模型不變。

診斷狀態新增 `displacement` 與 `movement_limit`，用來比較實際位移和接受門檻。

## 驗證

- 同一模型結果：0.1 秒內位移 0.25，OLD 為 `wrist_jump`，MID 為 `tracked`。
- 同一模型結果：短暫漏偵測後恢復，OLD 為 `stabilizing`，MID 為 `tracked`。
- 過快跳位、靠近另一手、錯誤標籤與衝突後重新確認仍有測試。
- 192 項測試通過。這是合成座標驗證，尚不能保證真人漏偵測已解決。

## 怎麼測

用 `MID/start_game.bat` 啟動，先讓兩手穩定入鏡，再逐漸加快遊戲零動作，完成後保存紀錄。與 OLD 比較時，盡量使用相同攝影機、距離及動作；不要同時啟動兩份遊戲占用攝影機。

若仍消失，查看回放追蹤原因：`no_detection` 是模型沒有該手；`low_confidence` 是分類信心不足；`wrist_jump` 是位移被排除；`identity_conflict` 是身分可能串錯。這些原因需要分開處理。
